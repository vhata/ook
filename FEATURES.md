# Features

Feature ledger. Grouped by milestone. One line per entry. Plain language.

Legend: ✓ shipped · ⋯ in progress

## Milestone — initial work

- ✓ Home page renders Currently Reading, Recently Finished, and the 2026 Bingo grid from the local vault path.
- ✓ Bingo grid shows title + first author per square, with a gold star on done squares and a `N / 24 squares done` counter.
- ✓ Per-book pages at `/books/[slug]` rendering body markdown plus optional review and quotes. Linked from home-page book cards and bingo cells with a known book. Every book is reachable.
- ✓ **Tiered spoiler model.** Tier 0 (catalog: title, author, status, rating, bingo, dates) always visible. Tier 1 (synopsis, review, quotes) server-rendered, click-to-reveal, per-session persistent. Tier 2 (deep notes) fetched client-side from `/api/books/[slug]/notes` only after explicit opt-in — never in initial HTML, so search engines don't index spoilers.
- ✓ TBR / Re-Read Aspirations section on the home page renders `_meta/tbr.md` body.
- ✓ Bare YAML dates in book frontmatter (`finished: 2026-02-20`) parsed into `YYYY-MM-DD` strings, fixing recently-finished sort order.
- ✓ Per-book pages show which bingo squares the book claims, with a link back to the bingo grid.
- ✓ Per-book pages render the book's tags as small badges.
- ✓ Per-book document title is `<Title> — <Author> · ook`; layout title template "%s · ook".
- ✓ Custom 404 page consistent with site styling (root + `/books/[slug]` segment variants).
- ✓ Global footer with link back to the GitHub repo, plus understated dot-separated links to `/shelf`, `/now`, and `/feed.xml` for chrome-level discoverability of the secondary public surfaces. Footer continues to suppress on `/now` via `SiteChrome`.
- ✓ Open Graph metadata for the site title, description, and type.
- ✓ Stats line under the home-page H1 (`N reading · M recently finished · X / Y bingo`).
- ✓ Visual overhaul to Claude Design's D3 prototype: paper-and-ink + charcoal palette, Source Serif 4 + Inter Tight + IBM Plex Mono, rust accent, bordered stats strip, large hero "Reading now" card, finished grid with covers, bingo with id chips and per-square cover slots, TBR with sub-pile filter pills, sticky-TOC book pages with pullquote and cross-links.
- ✓ Theme toggle (light / dark / auto) persisted in localStorage via `useSyncExternalStore`.
- ✓ Frontmatter extensions: `cover`, `pullquote`, `see_also`, derived `lastEdited` from git log.
- ✓ TBR pile parser (`## Wanted`, `## Re-Read Aspirations`, etc.) into typed entries; falls back to raw markdown body when piles are empty.
- ✓ `/log` route: reading log grouped by month, derived from each book's `started`/`finished` dates.
- ✓ Spoiler markdown directive (`:::spoiler ... :::` via remark-directive) renders click-to-reveal blur on per-book pages.
- ✓ Mobile responsive: bingo becomes a horizontal scroll-strip (5×5 stays sacred); finished cards stack horizontally; stats compress to 2-col with short labels; controls bar icon-only.
- ✓ Open Library cover URLs in frontmatter for all bingo squares + per-book directories. Bingo cell tooltip shows "title — author" on hover.
- ✓ Vercel deploy live at https://b-ook.vercel.app. Vault split into private `vhata/books` repo with deploy key + webhook on push for auto-rebuild.
- ✓ `bin/book` Node CLI in the vault for deterministic frontmatter mutations + `book list` (with vault / bingo / Goodreads sources, filters by status, tag, bingo, rated) + `book covers` (HTML grid of Open Library editions in the browser) + `book cover` (set chosen URL) + `book import-goodreads` (parse CSV, write to `_meta/goodreads.md`).
- ✓ Auto-promote bingo and Goodreads-only entries to vault directories on first mutation. Verification prompt for GR-claimed status / rating / date.
- ✓ Multi-year bingo support: current year derived from the highest `_meta/bingo-YYYY.md` filename; per-book pages attribute to whichever card actually references the book.
- ✓ External-link row on per-book pages — "View on Goodreads · Hardcover · Storygraph · Bookwyrm" rendered from optional `goodreads_id`, `hardcover_slug`, `storygraph_slug`, `bookwyrm_url` frontmatter fields. Missing fields produce no link; nothing is guessed.
- ✓ `/stats/[year]` route: yearly reading stats derived from frontmatter — finished count, abandoned, started-in-year, average rating with histogram, top tags, top authors, would-reread count. `/stats` index redirects to the most recent year with activity. Header includes a year-picker across all years that have data. Reachable from the controls bar.
- ✓ Manual log entries from optional `_meta/log.md` — non-book events (notes, "committed to bingo", "added to TBR", reread, progress) merged with frontmatter-derived started/finished events on `/log`. Schema: `## YYYY-MM-DD` headings, bullets prefixed with bold kind labels (`**Note** — body`).
- ✓ Per-route Open Graph share images via Next 16 `opengraph-image.tsx` file convention. Home card shows the wordmark + currently-reading + last-finished slots; per-book card shows cover, title, author, status, rating, finish date. Source Serif 4 fetched from Google Fonts at generation time so share-card typography matches the live site.
- ✓ `/random` route — redirects to a random finished book; reachable via the 🎲 button in the controls bar. Empty-vault fallback redirects home.
- ✓ `/feed.xml` Atom and `/feed.json` JSON Feed of finished books — discoverable from `<head>` so RSS readers auto-detect. Pullquote (or first review paragraph) used as each entry summary; finish date as `<updated>`/`date_published`. Cached for an hour.
- ✓ `/series` browser — every book with a `series` frontmatter, grouped by name, ordered by `#N` index when present. Each row shows cover thumb, status, rating, finish date. Reachable from the controls bar.
- ✓ Reading-day heatmap on `/stats/[year]` — GitHub-contribution-style calendar grid, 7×52 cells, intensity ramped by event count per day (started + finished + manual log entries). Tooltip shows date and count.
- ✓ "On this day" strip on the home page — surfaces reading-log events from past years that share today's month-and-day (started/finished/notes). Renders only when there's something to show; year tag + kind label + linked book title.
- ✓ `/stats` index now renders "Years in reading" — each year's first finished book and last finished book with rating + finish date, linking through to the year's full stats. Falls through to `/stats/[currentYear]` only when no year has data yet.
- ✓ `/discover` route — top-N most-connected book pairs in the vault, scored by see-also links, shared series, shared author, and overlapping tags. Per-pair reason chips explain the connection. Pure vault derivation, no external recommender.
- ✓ `/tags` index + `/tags/[tag]` — tag taxonomy with sized tag cloud, per-tag co-occurrence chips, and a per-tag book grid. Tag chips on per-book pages now link into the matching tag page.
- ✓ Reading-drought banner on `/log` — when the most-recent event is more than 21 days old, the page heads with a soft "X days since the last event — quiet stretch" callout.
- ✓ Polish: breathing accent dot on the home-page masthead (4-second opacity loop, gentler than the reading-now pulse), and a redesigned 404 page across both root and per-book routes — site palette, dropped-book line-art SVG, restored typography.
- ✓ "Threads" sidebar on per-book pages — top 3 algorithmically-similar books from `getSimilarBooks`, alongside the existing user-curated "See also" links. Each thread row shows the strongest reason (see-also · series · author · tag) and links into the full `/discover` index.
- ✓ Rotating pullquote on the home page — random `pullquote` from any finished book that has one, surfaced as a small italic block between the stats strip and the rest of the page. Re-rolled per page-load.
- ✓ Inline "X days quiet" markers between adjacent log entries on `/log` — companions to the top-of-page drought banner, surfacing in-month and cross-month gaps that exceed the 21-day threshold.
- ✓ Bingo `done`-ness derived from the bound book's status. Single source of truth — the YAML `done:` field is no longer trusted; a square is done iff its bound book is finished.
- ✓ ESLint encoding of "vault is read-only from src/" — `no-restricted-imports` + `no-restricted-syntax` ban `fs.writeFile`, `fs.appendFile`, `fs.rm*` (and `node:fs` / `fs/promises` equivalents) anywhere under `src/**`. The prebuild SSH-key writer in `scripts/` is out of scope.
- ✓ React-component test scaffold — `@testing-library/react` + `happy-dom` under vitest with `// @vitest-environment happy-dom` per-file. Initial coverage on `Spoiler` and `RevealSection` (the load-bearing tier-1/tier-2 reveal primitives).
- ✓ Serendipity card on the home page — a quiet "Remember this?" sidebar surfacing one finished book whose finish date is more than a year old. Re-rolled per page-load like the rotating pullquote. Cover thumb on wider screens; year-count callout in the kicker.
- ✓ Weekday vs weekend split on `/stats/[year]` — under the heatmap, two-row bar chart comparing per-day reading-event rate on weekdays vs weekends, with a "weekends run 2.3× hotter" / "even split, day for day" callout for the skew direction.
- ✓ Rating-over-time chart on `/stats` — inline-SVG dot plot of every rated finished book against finish date, with a rolling-average accent line (window scales with corpus size) and year ticks on the x-axis. Reveals taste drift, harshening, kindening. Renders only when there are at least four rated finishes.
- ✓ "Stuck" badge on per-book pages and home finished cards — surfaces books that earned the reader's full attention. Rule: finished + has a review + has quotes + (rating ≥ 4 OR would-reread). Three signals because any one alone is too easy to clear.
- ✓ "The wall" cover mosaic on `/stats/[year]` — every cover of every finished book in the year, tiled into a single block at the bottom of the year stats page. Auto-fill grid sized to the available width (≥64px tiles), in finish-date order. Hover-scales each tile; tooltips name the book.
- ✓ Per-book QR endpoint at `/books/[slug]/qr` — 512×512 PNG that encodes the canonical book URL in the paper-and-ink palette. Slug is validated against the vault; unknown slugs 404 so the endpoint can't mint QRs for arbitrary URLs. Cached aggressively (1d browser, 7d edge).
- ✓ Foxing-by-age CSS filter on finished-book covers — subtle sepia + slight contrast loss scaled by years-since-finish. Applied to home `FinishedCard` thumbs and the `/stats/[year]` cover mosaic. Capped at sepia(0.32) and contrast(0.92) so even ancient books stay legible. Computed in the parent server component so React's purity rule on `Date.now()` doesn't fire.
- ✓ Seasonal accent palette drift — the rust accent shifts gently across the year (rust in winter, ochre in spring, slate-blue in summer, forest in autumn). Implemented via a `<style>` tag in the root layout that overrides `--accent` and `--accent-soft` based on the server's current month. Paper-and-ink stays constant.
- ✓ Marginalia rendering for the quotes section — when `quotes.md` is revealed, blockquotes render with hand-drawn-feeling pencil-bracket SVGs on either side instead of the standard left-border. Blockquotes elsewhere on per-book pages are unaffected.
- ✓ Postal-stamp flourish on per-book pages — finished books get a small SVG stamp in the cover corner: title up top, star rating in the middle, author + postmark date at the bottom, perforated border. Slightly rotated. Doesn't replace the cover; sits as a thumbnail-sized seal next to it.
- ✓ Word-frequency cloud on `/stats` ("What you keep saying") — top words across every `review.md`, filtered against a stop-word list, sized by frequency. Renders only when the corpus has at least six non-singleton words.
- ✓ "Days in" indicator on Currently Reading cards — derived from each book's `started` date. Reads as a small uppercase caption under the author line.
- ✓ Finishing-patterns callout on `/stats` — consecutive-finish ordered pairs that occur ≥2 times across the reading log. Surfaces habits like "always reads A before B."
- ✓ Time-machine view at `/?at=YYYY-MM-DD` — renders the home page as it would have looked on a past date by filtering the current vault frontmatter through a date lens. Currently-reading, recently-finished, and bingo all re-derive against the lens date; the secondary surfaces (TBR, on-this-day, rotating quote, serendipity) hide so the page reads cleanly. Top-of-page banner with "back to today" link.
- ✓ `/shelf` — bookspine view of every finished book. Vertical SVG spines side-by-side, coloured from a hash of the first tag (or first author when untagged), title running upward, author at the foot, decorative bands at top + bottom. Sortable by finish date / author / rating / title.
- ✓ `/print/[year]` — printable year bibliography. Clean centre-anchored typographic layout — index number, title, author, finish date, optional pullquote, would-reread loop mark. `@media print` rules strip nav, force serif, and set A4 margins so Cmd-P → PDF produces a real-paper artefact.
- ✓ `/changelog` — what's been edited in the vault recently. Books grouped into Monday-anchored ISO weeks by their `lastEdited` date (already derived from `git log` on the reference file). Each row shows date, title link, status. Pure derivation from existing data — no new tooling needed in the vault.
- ✓ `/triage` — actionable inbox of books to decide on. Renders manual recommendations from `_meta/triage.md` only (same shape as `tbr.md` — H2 piles + bulleted entries); imported-but-not-fleshed-out Goodreads entries now route through `scripts/promote-goodreads.mjs` instead of surfacing here. Owner viewers get per-row buttons (Promote to TBR / Mark as reading / Mark as finished) plus a sticky bulk-action bar that submits the selection through `/api/admin/agent/commit-batch` as a single batched commit. Anonymous viewers see a read-only render. Each action removes the bullet from `_meta/triage.md` and either appends to `_meta/tbr.md` (promote) or mints `<slug>/<slug>.md` with the right status (start / finish), all in one atomic vault commit.
- ✓ `scripts/promote-goodreads.mjs` — shelf-aware bulk promoter for `_meta/goodreads.md` entries. `read` / `currently-reading` mint a per-book vault directory with the matching status (`finished` / `reading`); `to-read` appends a bullet to `_meta/tbr.md` under a dated `## From Goodreads (YYYY-MM-DD)` pile instead of minting a directory; anything else falls through to the legacy `status: tbr` directory path. The TBR lane is idempotent — bullets that already mention the same `goodreads_id` anywhere in `tbr.md` are skipped on re-run. Series extracted from `(Name, #N)` parentheticals, ratings / dates carried through. Defaults to dry-run with a shape summary; `--apply` writes (or answer `y` to the `maybePromptApply` prompt). Pure routing helpers live at `scripts/lib/goodreads-routing.mjs` and are pinned by unit tests.
- ✓ `scripts/import-triage.mjs` — generic CSV → `_meta/triage.md` converter. Loose column mapping (title required; author/why/source/pile optional). Supports `--append` to merge with an existing triage file.
- ✓ `/vault-health` page + `scripts/vault-lint.mjs` — same checks two ways. Surfaces missing-but-expected frontmatter (no `finished` date on a finished book, broken `see_also` references, no rating where one's conventionally expected). Severity tiered as error / warning / info; the route renders a faceted view, the script does CLI output for pre-push audits.
- ✓ Procedural covers for books without a `cover:` URL. Replaces the old crosshatched placeholder with a hash-coloured SVG: gradient background from the same eight-colour palette as `/shelf` (selected by hash of the title), oversized initials in the centre (skipping articles like "the"), title in small caps at the foot. Reads as intentional rather than missing-data.
- ✓ Per-book postcard PNG at `/books/[slug]/postcard.png` — 1200×800 sharable artefact via `next/og`. Cover floated left, title + author + pullquote on the right, accent stripe at the bottom, ook mark in the top-right corner, URL + date in the footer. Cached aggressively (1d browser, 7d edge).
- ✓ Share row on per-book pages — small "↓ QR · ↓ postcard" affordances under the external-link row, surfacing the QR and postcard endpoints that previously required URL-guessing. Each opens in a new tab.
- ✓ Corpus-level vault-health checks — an orphan detector (books with no incoming `see_also` references and no bingo binding) and an asymmetric-see_also detector (`A → B` where `B` doesn't link back). Both surface as info-level findings on `/vault-health` and through `vault-lint --severity info`. Same checks two surfaces, mirrored between `src/lib/vault-health.ts` and `scripts/vault-lint.mjs`.
- ✓ `scripts/backfill-see-also-bidirectional.mjs` — closes the loop on `see_also` reciprocity. For every `A → B` in the vault where `B` doesn't link back, proposes adding `A` to `B`'s `see_also` (additive only — never bumps existing entries; respects MAX_SEE_ALSO). Designed to run after the other see_also backfills; pure derivation, idempotent, dry-run by default. Wired into the `vault-backfill` umbrella.
- ✓ Component test coverage for `DeepNotes` and `Cover` — the tier-2 fetch-on-click reveal contract is now pinned end-to-end (mocked fetch, error retry, sessionStorage rehydrate), and the procedural-cover fallback is pinned at the hash-colour and initials-derivation level. `vitest.config.ts` added to mirror the `@/*` tsconfig alias so components can be tested without rewriting their imports.
- ✓ `/schema` route — auto-documented corpus shape. For every field the renderer reads, shows coverage (X of Y books populated, with a horizontal bar), kind, and up to three sample values. Companion to `/vault-health`: that page shows what's missing per book; this one shows the schema as a whole. Marked `noindex`. Powered by `src/lib/schema.ts` (pure derivation from typed Book records).
- ✓ "Strongest pairings" section on `/tags` — top 10 tag pairs by co-occurrence count across the corpus, rendered above the existing tag cloud as a bar-graph list. Each side of a pair links to its own `/tags/[tag]` page. Pure derivation via `computeTagPairs` (alphabetical canonicalisation, count-desc sort, configurable minCount/limit), exposed as a pure helper for tests and called from the async `getTagPairs` wrapper.
- ✓ Books-repo webhook → `/api/webhooks/books/reindex`. Push to `vhata/books` triggers GitHub to POST a signed payload (HMAC-SHA256 over `X-Hub-Signature-256` using `OOK_BOOKS_WEBHOOK_SECRET`); the route verifies the signature and calls the same `reindex()` the admin button uses. Keeps the MCP store fresh without manual nudges; replaces the post-Upstash-provisioning footgun where the store sat empty until someone clicked Reindex. Lives outside the session-cookie proxy matcher — HMAC is the access control.
- ✓ Patch handler frontmatter fidelity. `applyPatch` (the MCP write surface) now performs surgical line-level rewrites on the YAML frontmatter rather than re-serialising the entire block. Adding a single tag produces a commit that touches only that one line, matching the diff preview the user confirmed. `parseMarkdownFile` extracts the raw block via regex (gray-matter v4 doesn't expose `parsed.matter`, so the preserve-raw path was previously a no-op). Six new fidelity tests pin the behaviour.
- ✓ Multi-series schema support. The vault already encodes books with multiple series memberships via `; `-delimited strings (`series: "Discworld, #32; Tiffany Aching #2"`); previously both the renderer and the backfill scripts butchered them. `parseSeriesMemberships(raw)` is now the canonical parser — used by `getAllSeries` (a book lands under each of its series sub-trees) and mirrored into `backfill-see-also.mjs`, `backfill-tags-from-peers.mjs`, and `backfill-see-also-from-tags.mjs` so series-mate detection and diversity dedup count every membership. `parseSeriesField` is preserved as a thin wrapper returning the first membership for back-compat.
- ✓ Per-book page integration tests. The `/books/[slug]` server component now has end-to-end coverage pinning the tier-0/tier-1/tier-2 reveal contract: catalog facts (title, author, status, rating) render in initial HTML; review and notes are gated behind reveal buttons; the share row exposes QR + postcard endpoints; an unknown slug throws `NEXT_NOT_FOUND`. Mocks `next/link`, `next/navigation`, and the data layer; renders the awaited server component output through `@testing-library/react`.
- ✓ Multi-series rendering on per-book pages. Books with `; `-delimited series strings now render as a dot-delimited list of memberships ("Discworld #32 · Tiffany Aching #2") instead of the raw YAML string. Powered by `parseSeriesMemberships`; renders nothing for books without a series.
- ✓ Sub-series detection on `/series` — when every member of series X is also in larger series Y, X gets a "sub-series of Y" badge with an in-page anchor link to the parent. Powered relationally from corpus data; no schema needed. Tiffany Aching, Witches, Watch, etc. now read as Discworld sub-series.
- ✓ Series memberships in the per-book Toc sidebar. The sticky sidebar now leads with a Series section listing every membership and its index, each linking to its `/series` anchor. Stays visible while scrolling deep notes.
- ✓ Gap placeholders on `/series`. Conservative integer-gap detection between known indexes — if the vault has Discworld #1, #3, and #5, the renderer interleaves dashed-border `(not in the vault)` placeholders at #2 and #4. Decimals are ignored (a `#1.5` doesn't fill the surrounding integers); never invented past the lowest or highest known index.
- ✓ Hybrid Hardcover series rosters. New `scripts/backfill-series-rosters.mjs` (run via `make vault-series-rosters` / `make vault-series-rosters-apply`) fetches each series the vault knows about from Hardcover's GraphQL API and writes `_meta/series-rosters.json` to the vault. The `/series` renderer reads that cache at request time: when present, missing entries render with their canonical title + author from Hardcover; the header denominator becomes the canonical total ("3 of 41 read" instead of "3 of 3 read in vault"). When no roster exists for a series, falls back to the corpus-only gap detection. Internet only at user-initiated `make` time; build stays offline-clean.
- ✓ `/robots.txt` and `/sitemap.xml` via Next 16 file conventions (`src/app/robots.ts`, `src/app/sitemap.ts`). Robots allows the public surfaces, disallows `/vault-health`, `/schema`, `/api/`. Sitemap enumerates every public route — home, log, stats, series, shelf, discover, tags, triage, changelog, random — plus every book page, every bingo year (`/print/[year]`), and every stats year. Per-book `lastModified` reads from the existing `lastEdited` field.
- ✓ Canonical Make targets for vault hygiene — `make vault-lint`, `make vault-backfill` (dry-run), `make vault-backfill-apply` (writes). The umbrella runs every backfill in dependency order: `source`, `tags` (Open Library), `tags-from-peers` (corpus-derived), `see_also` (series + author), `see_also-from-tags` (tag-Jaccard).
- ✓ Two corpus-internal backfill scripts that derive enrichment from the vault alone — no network. `scripts/backfill-tags-from-peers.mjs` proposes tags for thinly-tagged books by tallying their series-mates, same-author books, and see_also peers under signal-source-aware thresholds (series-fraction ≥ 0.66, author-fraction ≥ 0.8 with min count 3, see_also count ≥ 3). `scripts/backfill-see-also-from-tags.mjs` extends `see_also` via tag-Jaccard similarity (min 3 shared tags, Jaccard ≥ 0.4) with diversity caps (≤ 1 entry per series, ≤ 2 per author) so a heavily-tagged series can't crowd out other recommendations. Both default to dry-run.
- ✓ `/admin/audit` — passkey-gated audit log of recent vault commits. Shells out to `git log` against the cloned `.vault/` (same `execFile` shape as `getLastEditedMap`), parses the last 50 commits with TAB-delimited fields plus `--shortstat` for files-changed counts, and renders a list with date, subject, author, file count, short SHA, and a deep-link to the commit on `vhata/books`. Unauthenticated requests bounce to `/admin`. Cross-linked from the admin console as a quiet "Recent activity →" affordance next to Reindex / Sign out. Closes the "did that commit go through?" loop without leaving the site for GitHub. Empty-state when `.vault/` isn't a git repo (dev without `BOOKS_DEPLOY_KEY`). Parser pinned by unit tests. Footer carries the store's `lastReindex` stamp (relative time) so a stale-looking commit list can be disambiguated from a stale reindex at a glance; silent-omit when the key is absent (dev without a store, fresh deploy before the first reindex).
- ✓ Webhook-driven vault hygiene. After the books-repo webhook reindexes the MCP store, it fires a `repository_dispatch` event on the ook repo to kick off `.github/workflows/vault-hygiene.yml`, which checks out both repos, runs `make vault-lint` (fail-loud), runs the offline-fast backfills with `--apply` (`source`, `tags-from-peers`, `see_also`, `see_also-from-tags`, `see_also-bidirectional` — Open Library tags stay operator-initiated), and pushes a single `Auto-hygiene:` commit if anything changed. Webhook handler filters non-main pushes and short-circuits the dispatch when the head commit message starts with `Auto-hygiene:` so the workflow's own commits don't trigger redundant runs. Concurrency-grouped so rapid pushes coalesce to one run.
- ✓ Navigable `/series` — sticky left-rail TOC on desktop, horizontal scroll-strip of series chips on mobile, and `<details>`-based collapse-by-default on every section. Sub-series indent under their parent in the TOC. `?expand=all` and `?collapse=all` query params drive an Expand-all / Collapse-all toggle near the page header — server-rendered, no JS island. Real `id` anchors so back/forward and copy-link sharing work; `scroll-mt-6` keeps the header visible when jumping into a section.
- ✓ Interactive prompt-to-apply on every backfill script. When stdin is a TTY and the dry-run produced pending changes, each script prints the summary and asks `Apply these N changes? [y/N]`; on yes, it writes from the changeset already computed (no re-deriving, no re-fetching from Open Library or Hardcover). Non-TTY stdin (CI, pipes, redirected input) skips the prompt and exits dry-run cleanly. Shared helper `scripts/lib/maybe-prompt-apply.mjs` keeps the prompt logic out of every script; covers `backfill-source`, `backfill-tags`, `backfill-tags-from-peers`, `backfill-see-also`, `backfill-see-also-from-tags`, `backfill-see-also-bidirectional`, `backfill-series-rosters`, and `backfill-hardcover-books`.
- ✓ `SeriesHashOpener` client island on `/series` — opens any `<details>` whose `id` matches `window.location.hash` (and re-runs on `hashchange`). Closes the loop where a per-book Toc link to `/series#series-discworld` would otherwise scroll to a closed disclosure. Pure side-effect, renders nothing.
- ✓ Kindle `My Clippings.txt` one-shot importer (`scripts/import-kindle-clippings.mjs`, run via `make vault-import-kindle FILE=…` / `make vault-import-kindle-apply FILE=…`). Parses the clippings file (UTF-8 with BOM and UTF-16-LE both supported, CRLF tolerated), fuzzy-matches each highlight's Kindle title to a vault directory (high precision: exact normalised equality, then conservative substring containment with length floors), and appends new entries into the matched book's `quotes.md` under a `## From Kindle` block (Notes go to `## Notes from Kindle`; Bookmarks are dropped). Each entry carries a stable 16-char hash in an HTML comment so re-running the importer never duplicates. Unmatched titles land in `_meta/kindle-unmatched.md` for triage. Pure parsing, matching, dedupe, and rendering helpers live in `scripts/lib/kindle-clippings.mjs` and are pinned by unit tests.
- ✓ Bookcision JSON one-shot importer (`scripts/import-bookcision.mjs`, run via `make vault-import-bookcision FILE=…` for a single file or `make vault-import-bookcision DIR=…` for a directory of exports). Targets the Bookcision bookmarklet's `{ asin, title, authors, highlights[] }` JSON shape — the primary path for highlight import on Kindle-for-Mac, where `My Clippings.txt` is not produced. Joins each payload to a vault directory via `amazon_asin:` first, falling back to fuzzy title match against vault `title:` frontmatter (same precision floors as the clippings importer). Appends new highlights into the matched book's `quotes.md` under a stable `## From Kindle highlights` heading; per-entry HTML-comment dedupe hashes (`<!-- bookcision-hash:… -->`) make re-runs no-ops. Per-file idempotency state at `_meta/bookcision-state.json` short-circuits unchanged files in `--dir` mode without re-reading their corresponding `quotes.md`. Pure parsing, matching, rendering, and state helpers live in `scripts/lib/bookcision.mjs` and are pinned by unit tests.
- ✓ Pages-derived stats on `/stats/[year]` — Topline now carries a "Longest" metric (the longest finished book of the year by Hardcover-`pages`, with title + author hint), and a 12-bar pages-per-month chart sits below the weekend-split section. Both surfaces degrade silently when the Hardcover cache has no `pages` for the books in question (no metric, empty chart skipped). Source data is the existing `_meta/hardcover-books.json`; no new external fetches.
- ✓ "Pages" total in the `/stats/[year]` Topline strip — sum of Hardcover `pages` across every finished book in the year that has a paged record, rendered with comma separators next to Avg rating, with a "from N of M books with page data" hint so the coverage gap is visible. Tile is omitted entirely when no finished book in the year has a paged Hardcover record (silent degrade, same shape as the Longest metric).
- ✓ Reading-velocity ETA on Currently-Reading cards — when the home-page reading book has `hardcover.pages` and the user has finished books with `pages` data in the last 3 months (falling back to 12 months when 3 is empty), the card adds an "≈ N days at your pace" caption. Pace is `total pages / number of distinct days that had a finish`, not days elapsed in the window — counts the days the reader actually closed a book. Skipped under the time-machine lens (pace doesn't make sense in past-views) and absent for books without paged Hardcover records.
- ✓ `scripts/backfill-hardcover-ids.mjs` — copies `hardcoverSlug` and `hardcoverId` from the existing `_meta/hardcover-books.json` cache into each book's frontmatter as `hardcover_slug` / `hardcover_id`. Pure cache-to-frontmatter (no network), surgical line-level writes (no whole-block re-serialisation), per-field skip so a manually-set slug or id is never overwritten even when the cache disagrees. Wired up as `make vault-hardcover-ids` (dry-run) / `make vault-hardcover-ids-apply`. Closes the gap where the renderer's outbound external-link row was silently skipping Hardcover for every book the user hadn't manually annotated.
- ✓ `/now` — minimal embeddable "what I'm reading right now" surface. Three signals only: currently-reading book(s), the most recently finished book, and the current ongoing reading streak (rendered when ≥ 2 days). No nav, no controls bar, no footer chrome — `SiteChrome` (a small `usePathname`-aware wrapper inside the root layout) suppresses the global wrapper for this route so the page can be iframed cleanly into a personal homepage or shared as a now.html-style link. `revalidate = 300` (5-minute stale-while-revalidate); `robots: noindex,nofollow`; no `?at=` time-machine lens (live signal only). Streak helper `getCurrentReadingStreak` lives next to `getCurrentlyReading` in `src/lib/books.ts`; counts back from today (or yesterday if today has no events yet) over started/finished/manual-log dates.
- ✓ Vault → Hardcover one-way status sync. New `scripts/sync-hardcover-status.mjs` (run via `make vault-hardcover-sync` / `make vault-hardcover-sync-apply`) pushes the vault's `status`, `rating`, `started`, and `finished` fields up to the operator's Hardcover account via GraphQL `insert_user_book` / `update_user_book` mutations and `upsert_user_book_reads` for read-session dates. Goodreads' write API has been dead since 2020-12-08; Hardcover is the only viable target. Idempotent through two layers: a local `_meta/hardcover-sync-state.json` cache short-circuits no-op re-runs, and a remote diff check before every mutation prevents double-writes when the cache is stale. Vault is the source of truth — a flip from finished back to tbr still pushes — but Hardcover ratings finer than the vault's integer scale are preserved when the difference is ≤ 0.5. Wired into `.github/workflows/vault-hygiene.yml` so every vault push automatically syncs when the `HARDCOVER_TOKEN` repo secret is populated.
- ✓ "What others said" disclosure on per-book pages — top 2-3 short, high-rating, non-spoiler reviews from Hardcover, surfaced inside a click-to-reveal section that sits alongside the existing review/quotes/synopsis tier-1 reveals. New `scripts/backfill-hardcover-reviews.mjs` (run via `make vault-hardcover-reviews` / `make vault-hardcover-reviews-apply`) queries Hardcover's `user_books` table — Hardcover has no `book_reviews` root type, reviews live on the per-user shelf row — filters server-side for `has_review`, `rating ≥ 3`, `review_length` 80..600, and `review_has_spoilers: false`, and writes the top 3 by `likes_count` to `_meta/hardcover-reviews.json` keyed by vault slug. Bodies arrive as HTML and are stripped to plain text before caching. Per-book opt-out via `hide_external_reviews: true` in frontmatter; books without qualifying reviews don't render the section at all. Same operator-initiated, vault-committed-cache discipline as the other Hardcover scripts.
- ✓ MCP commits carry a `via ook-admin/<session-id>` trailer so `/admin/audit` can distinguish writes that flowed through the in-process MCP tools (the passkey-gated `/admin` console + the external MCP HTTP transport) from direct pushes to `vhata/books`. The trailer is appended once at module load — the session id is a 7-char slice of `crypto.randomUUID()`, stable for the life of the function instance — and rides at the end of the user-supplied commit message, separated by a blank line. Idempotent: re-tagging an already-tagged message is a no-op. The audit parser reads the body via a NUL-record `git log -z` with `%b` in the format and extracts the session id when the trailer is on the final non-empty line; the audit page renders a small "via MCP" chip on those rows. Chip says "MCP" rather than "/admin" because both surfaces flow through the same tools. Parser pinned by unit tests covering subject-only mentions, mid-body mentions, and idempotent re-tag.
- ✓ Finish-flow pullquote + rating gate on `/admin`. When the user reports finishing a book, the admin agent asks for a pullquote and a star rating before staging the status flip, then bundles all three changes into a single `propose_patch` call so one commit lands on `vhata/books`. The gate lives in the agent's system prompt (natural language — the tool layer doesn't enforce it), is default-on but operator-overrideable ("just mark it finished, skip the rating"), and treats explicit refusal of both questions the same as a tab close: the status flip is lost, by user-stated design. To make the ask/answer/ask/answer/commit shape actually work, the `/api/admin/agent` endpoint now round-trips an opaque `state` handle (the Anthropic SDK `messages` array) and the `AdminConsole` carries it forward across follow-up submits — single-turn for fresh requests, multi-turn for clarifications. Patch-handler bundling is pinned by a `applyPatch` unit test; the system-prompt instruction block is string-pinned in `admin-agent-prompt.test.ts` (behavioural coverage of the gate itself would need a mock Anthropic transport — manual-test recipe documented inline in the same file).
- ✓ `/admin/backfill` — passkey-gated "fill the gaps" view that picks 3-5 small, skippable questions per visit about older finished books missing one piece of metadata each. Three kinds at launch: a rating prompt for finished books with no rating; a one-or-two-sentence review prompt for finished books rated ≥ 4 with no `review.md`; a yes/no would-reread prompt for 5-star finishes where the field is null. Candidates from each kind are shuffled independently then interleaved round-robin so a single visit always sees a mix; dedupe by slug keeps any one book from appearing twice on a visit. Saves post a structured `CommitPatchInput` straight to the existing `/api/admin/agent/commit` route — no Claude tokens burned for a "set rating=4" deterministic update — so every backfill commit picks up the same `via ook-admin/<id>` trailer and audit-log entry as a `/admin` console write. Skip is pure client-side state; a "that's all for this visit" footer surfaces when every card is resolved. Cross-linked from the admin console next to Recent activity. Pure derivation from `getAllBooks` via `src/lib/admin/backfill.ts`; no network at request time. `pickQuestions` is exposed for tests and pinned end-to-end on candidate selection, kind interleave, slug-level dedupe, and the empty-corpus fallback.
- ✓ Year-end pace projection on `/stats/[currentYear]` — small italic line under the Topline reads "on pace for ~N by year-end (F finished, M months in)" when the viewed year is the current calendar year, the user has at least 3 finishes so far, and we're at least 30 days into the year. Pure derivation from `finished / day-of-year × days-in-year`, rounded half-up. Suppressed silently on past years, sparse early-year data, and the final day of the year (nothing left to project). Computed via a pure `computePaceProjection` helper exposed for unit tests; `getYearStats` takes an optional `today: Date` for date injection, matching the pattern already used by `estimateReadingDaysRemaining`, `getOnThisDay`, and `getSerendipity`.
- ✓ Colored unified-diff output in vault-script dry-runs. Each frontmatter-mutating backfill (`source`, `tags`, `tags-from-peers`, `see_also` series+author / from-tags / bidirectional, `hardcover-ids`) now prints a per-book block shaped as a unified diff — `→ <slug>` header, then a red `- <old line>` / green `+ <new line>` pair for line replacements (or just the green `+` for pure insertions). Plain `-` / `+` markers carry the same signal when stdout isn't a TTY (CI logs, redirected output, pagers without `-R`), so golden files and grep stay stable. Honours the de-facto-standard `FORCE_COLOR` / `NO_COLOR` env vars so child processes can opt in without a real terminal. Shared helper at `scripts/lib/diff-format.mjs`, pinned by unit tests + an integration test on `backfill-hardcover-ids` that spawns the script with `FORCE_COLOR=1` and asserts the green-wrapped insertion lines flow through.
- ✓ Batch commit endpoint at `/api/admin/agent/commit-batch` — accepts `{ patches: CommitPatchInput[], message? }` and lands the whole list as a single vault commit. Shared by `/triage` bulk actions and the staged-answers footer on `/admin/backfill`. Same passkey gate as the per-patch endpoint (proxy matcher `/api/admin/*`). All-or-nothing validation: every patch is applied + checked (title non-empty, status remains a valid `BookStatus`, authors remain a string array) before any write begins; the first invalid patch rejects the whole batch with zero partial writes. Production path goes through the GitHub Git Data API (create blob per file → create tree on top of the branch head → create commit → fast-forward the ref) so a multi-file batch lands as a single SHA in `vhata/books`. Local-fs dev path writes each file sequentially. One `via ook-admin/<id>` trailer per batch commit (not per patch). Optimistic store update fires per slug in the batch, same as the per-patch endpoint. Response shape mirrors `commit/`'s, with an added `batchSize` count and per-patch `previews` array. `commitPatchBatch` helper in `src/lib/mcp/patch-batch.ts`; tests cover the all-or-nothing path, partial-write absence after a rejected batch, the single-trailer guarantee, the no-op short-circuit, and route-level validation (mocking the helper so the route is tested in isolation).
- ✓ `scripts/backfill-covers.mjs` — populates empty per-book `cover:` frontmatter URLs from the Hardcover cache, with Open Library and Google Books fallbacks for books the cache doesn't reach. The Hardcover lookup script also stores `image_url` in `_meta/hardcover-books.json` (one extra GraphQL field — `Book.image.url`, with `default_cover_edition.image.url` as fallback); the script reads that cache for the no-network primary path. The Open Library fallback queries by `isbn13` → `isbn` → title+author and accepts the first non-placeholder URL it finds (HEAD-probe with `content-length ≥ 3KB` to reject the ~1KB "no cover" placeholder), ranks search docs by language/region preference (English default; honours `language:` / `region:` / `edition:` frontmatter when present), and keeps a marginal ISBN13 candidate as a last-resort floor instead of emitting null. Google Books Volumes API (`?q=isbn:<isbn13>` then `?q=intitle+inauthor`) is the secondary fallback when Open Library has nothing usable — no auth, no token, same offline-clean discipline as the rest of the script. Rate-limited at ≥ 100ms between external requests. Per-field skip — any populated `cover:` value, including a manually-set URL, is never overwritten; `cover: null`, `cover:`, and `cover: ""` all count as empty. Wired up as `make vault-covers`. Closes the gap that left ~250 of ~280 books rendering the procedural foxing-colour tile because the cover URL was never automatically derived.
- ✓ Makefile section dividers — `make help` now renders bold uppercase headers (CORE / VAULT / DEPLOY) between the three target groups instead of one flat list. Section markers in the Makefile (`## --- core ---` etc.) are picked up by the `help` recipe's awk rule and rendered as a blank line + bold heading + blank line above the targets that follow; per-target lines keep their existing two-column shape. Driven by the same Makefile that names the targets — no sub-Makefile split, no subcommand pattern.
- ✓ "READ" stamp on done bingo squares. The old single ★ in the corner was too quiet against busy covers; done squares now wear a small accent-bordered postal-stamp chip ("★ READ" in small caps) in the top-right corner. Echoes the visual language of the per-book `Stamp` component (paper-and-ink, accent border, small caps) at chip scale so the marker reads as a celebration without obscuring the cover image. The reading-state "now" pill and the unread cover-dim treatment are unchanged. `BingoCellEl` lifted out of `src/app/page.tsx` into its own `src/components/BingoCell.tsx` for direct test coverage; the new test pins the stamp's presence rules (done → yes, reading-but-not-done → no, unread → no, free → no).
- ✓ Owner-aware public surfaces. When the viewer holds a valid session cookie, public pages render small inline admin affordances next to the rows where they make sense: "remove from series" on `/series` rows, "edit →" on `/books/[slug]`, "remove tag" on `/tags/[tag]` cards, and "edit entry" on `/log` rows. Each affordance is a link to `/admin?focus=<kind>:<id>&intent=<verb>:<detail>`, never a write endpoint — every actual mutation still flows through `/api/admin/agent` and the diff-preview gate. The /admin page reads those query params and pre-fills the agent textarea with a focused opener (e.g. "Edit piranesi: remove 'Discworld' from the series field."). The global Controls bar gains an `<AdminControls>` server component that renders the "admin" indicator, a quiet `/admin` link, and a sign-out form — visible only when authed, absent otherwise; anonymous viewers see the public site exactly as before. A new `getOwnerSession()` helper in `src/lib/auth/session.ts` centralises the `cookies()` + `verifySession()` dance every auth-aware route otherwise duplicates; the existing /admin, /admin/audit, and /admin/backfill pages now go through it. Logout route dual-modes its response (JSON for the existing `fetch`-based caller in the admin console, 303 redirect for the form-style sign-out from the Controls bar). No per-book lock fields, no denylist arrays: the friction of inline edits is the pressure that keeps manual corrections rare and surgical.
- ✓ Staged batched commits on `/admin/backfill`. Each card's "Save" became "Stage" — staging moves the answer into a client-side queue and visually marks the card (accent border + a "staged" pill). A footer (sticky on mobile, inline at the bottom on desktop) shows "N answers staged" with a "Send all" button that POSTs the queued `CommitPatchInput[]` to `/api/admin/agent/commit-batch` as one atomic vault commit, and a "Discard all" link that drops the queue and returns every staged card to its pending-editable state. A staged card can be re-edited (Edit → change the answer → Stage again) before "Send all" fires; Skip and dismiss stay purely client-side, unchanged from the per-patch flow. The staging queue lives in component state only — no localStorage, no server-side persistence — so leaving the page drops it by deliberate design (acceptable on a light-touch surface that already follows the "few questions per visit" shape). On a Send-all failure every staged card rolls back to staged so the user can retry, with the error surfaced in a footer alert. Replaces five per-card commits with one batch commit per visit; the `via ook-admin/<id>` trailer rides once on the batch message rather than per patch. Audit-hint trailer extension (`batch-size=N`) deferred per the TODO entry. Tests pin the wire-format builder (selected answers → `CommitPatchInput[]`), the initial Stage-on-every-card render, the stage/edit/restage cycle, Discard-all, the batched POST body, and the failure rollback.
- ✓ Actionable `/triage` page. Owner viewers (passkey-gated) get a per-row action set on each manual triage entry — Promote to TBR, Mark as reading, Mark as finished — plus a sticky bulk-action bar that applies one action across a checkbox-selection in a single batched commit. Anonymous viewers see the same read-only render as before. Promote-to-TBR removes the bullet from `_meta/triage.md` and appends it under a dated `## From Triage (YYYY-MM-DD)` pile in `_meta/tbr.md`; Mark-as-reading / Mark-as-finished remove the bullet and mint `<slug>/<slug>.md` with `status: reading`/`status: finished` and today's date plus `source: triage`. Both effects land in one atomic commit via `/api/admin/agent/commit-batch`. The Goodreads-unfleshed section is gone — those entries now route through `scripts/promote-goodreads.mjs` directly to TBR or a vault directory based on shelf. Pure wire-format builder at `src/lib/triage/actions.ts` (entry + action + today → `meta_patches`); client island at `src/components/TriageActions.tsx` owns the row + bulk UX. Tests pin the per-action patch shape (`remove-bullet` + `append-bullet` for promote, `remove-bullet` + `create-file` for the mint paths), the anonymous-vs-authed render contract, the bulk-selection POST body, and the per-row POST body.
- ✓ Meta-patch lane on `/api/admin/agent/commit-batch`. The batch endpoint body grew an optional `meta_patches: MetaPatch[]` field alongside `patches`, covering vault writes that aren't book-reference notes — pile-bullet edits on `_meta/triage.md` / `_meta/tbr.md` and brand-new `<slug>/<slug>.md` files minted by the `/triage` actions. Three patch kinds: `remove-bullet` (locates the bullet by literal content under a named H2 heading and drops it), `append-bullet` (lands a bullet under a named heading, minting the heading at the end of the file when absent), and `create-file` (writes a brand-new file, refuses when the path already exists). Same all-or-nothing discipline as the book-patch lane: every meta-patch is checked against the current on-disk file before any write begins; the first inconsistency rejects the whole batch. Multiple meta-patches against the same file compose correctly through an in-batch `pending` map so a single batch can remove-then-append on `_meta/tbr.md` without re-reading stale content. At least one of `patches` or `meta_patches` must be non-empty. Schema at `src/lib/mcp/meta-patch.ts`; the route accepts a snake-case `meta_patches` key, the helper takes camelCase `metaPatches`. Pinned by unit tests on the meta-patch helpers, the batch path's end-to-end meta-patch handling, and route-level acceptance of meta-patch-only bodies.
- ✓ `paused` status distinct from `abandoned`, with render-time auto-promotion. `BookStatus` gains a fifth value (`paused`) and a new `last_progress` frontmatter date drives a pure `effectiveStatus` helper at `src/lib/status.ts`: a `reading` book whose `last_progress` is more than 90 days stale renders as paused without rewriting the vault; explicit `status: paused` always wins. The `/now` page splits into two sections — full-width Reading cards with the accent ring for fresh books (< 14 days), and a quieter "Set aside" section with half-size cards, a `Nd quiet` indicator after the author, and owner-only "Pick it back up" / "Move to shelf" actions that POST single-patch batches to `/api/admin/agent/commit-batch`. Anonymous viewers see the two sections without the action buttons.
- ✓ Book summary tier shift — premise visible, running notes opt-in. New `premise:` frontmatter field renders as a small always-visible tier-0 paragraph under the metadata strip on per-book pages: a sentence or two in back-cover register, populated automatically from the Hardcover `description` field rather than hand-typed. The previous tier-1 summary RevealSection is gone; the running-notes file (renamed `summary.md` → `progress.md` to match its actual role — what the reader writes WHILE reading as a memory aid) now reaches the page only through the existing "show full notes (spoilers)" tier-2 opt-in, where it's prepended to the deep-notes payload under a `## Reading notes` heading. Reading-state and finished-state books behave identically on the renderer — the rule is status-blind. Type plumbing carries `hasProgress` through `src/lib/types.ts`, the `scripts/build-index.mjs` parse, and the walk-fallback parser in `src/lib/books.ts`; the MCP write surface knows `progress` as a file-backed section name.
- ✓ `summary.md` → `progress.md` vault rename. Operator-side `scripts/rename-summary-to-progress.mjs` (run via `make vault-rename-summary-to-progress`) renames every `<slug>/summary.md` in the vault to `<slug>/progress.md` — a one-shot migration to align on-disk names with the running-notes semantics. Default dry-run; `--apply` performs the rename; prompts at the end of the dry-run when TTY. Refuses to overwrite an existing `progress.md` (caller's responsibility to reconcile).
- ✓ Archive-on-finish. When the finish-flow gate fires (status → finished, with the pullquote + rating bundled in), the same `propose_patch` also archives the book's `progress.md` — a `create-file` for `_meta/progress-archive/<slug>.md` carrying the source content, plus a `remove-file` for `<slug>/progress.md`. Both meta-patches land in the same commit as the status flip via `commitPatchBatch`, so the archive and the source delete are atomic. The live vault retains only the reader's finalised voice (pullquote, review, quotes, marginalia, frontmatter) on finished books — the transient running aid lives on in `_meta/progress-archive/` for retrospective lookups. Schema additions: `MetaPatch` gains `remove-file { kind, path }`; `MultiFileWrite.content` is now `string | null` (null = delete sentinel) handled by both vault-client adapters; the GitHub adapter passes the deletion to `createTree` with `sha: null`. `/api/admin/agent/commit` accepts an optional `meta_patches` field and routes via `commitPatchBatch` when present. The finish-only rule is encoded in the agent's system prompt — abandoned and paused books keep their `progress.md`, by user-stated design (might pick them back up).
- ✓ Premise backfill from the Hardcover description cache. `scripts/backfill-hardcover-books.mjs` now pulls the `description` field on each `books` GraphQL record and caches it in `_meta/hardcover-books.json`; new `scripts/backfill-premises.mjs` ferries the cached description into each book's `premise:` frontmatter as a folded YAML block scalar (`premise: >-` + indented wrap). Pure cache-to-frontmatter, no network at runtime. Per-book skip: an existing `premise:` is user-authoritative and never overwritten, so manual edits stick. Operator-run via `make vault-premises` (the cache lookup remains operator-initiated via `make vault-hardcover-books`). Closes the "no homework" tenet on premise capture — the cover copy for a published book is well-known and non-spoiler, so the user no longer types it.
- ✓ Left-rail italics on `/series` for series with zero finished members. Top-level series with no member yet in `finished` state render italic in the desktop TOC and on the mobile pill strip, so "shelved but not started" reads differently at a glance from "in progress" or "fully read". Sub-series were already italic to encode their nesting; the new state is purely additive on the top-level rows. Computed at render time via a `hasNoFinishedMembers` predicate over the existing `SeriesGroup.members`; no schema change. Test in `series-page.test.tsx` pins the italic-on-unstarted / no-italic-on-started invariant.
- ✓ `/tags` cloud size ratio cap. The tag-cloud sizing function (`src/lib/tag-cloud.ts`) now maps each tag's count through `Math.log1p` and linearly scales the result into a fixed `[0.85rem, 1.7rem]` range using the corpus min and max as the endpoints — so the visual ratio between the largest and smallest tag is capped at ~2× regardless of how skewed the underlying counts are. Before the cap, `fantasy: 129` rendered ~3× the size of the next-tier tag and drowned out everything below it; after, the typographic spread stays legible across the whole cloud. All-equal counts pin to the floor (no spread to scale across); inputs outside the [min, max] window clamp into the size envelope. Pure helper pinned by unit tests on the equal-counts floor, the 1-vs-1000 ratio cap, the real-corpus shape (fantasy:129 vs a 1-count tag), monotonicity, and out-of-range clamping.
- ✓ Heterogeneous bulk on `/triage` — per-row action selector. Each manual triage entry now gets its own action select (`— no action — / Promote to TBR / Start reading / Mark finished`); the row's choice is queued in component state and a footer queue bar shows a running "N actions queued" count with a single "Send N actions" submit and a "Discard all" reset. One click can promote three rows, start reading two, and finish one — every queued row composes into the same `meta_patches` list against `/api/admin/agent/commit-batch` and lands as a single vault commit. Replaces the previous checkbox + per-row buttons + global bulk-action dropdown shape: a row's action selector subsumes both per-row and bulk affordances, and "no action" rows are leave-alone (excluded from the submit). Stage state lives in component state only — leaving the page drops it, consistent with `/admin/backfill`. New `buildHeterogeneousTriageBatch` pure helper at `src/lib/triage/actions.ts` composes the per-entry meta-patches and emits a mixed-action commit-message summary ("Triage: 6 actions (3 promoted, 2 started, 1 finished)"); single-entry batches still name the book in the audit log, and a homogeneous-action batch falls through to the same summary the previous single-action path emitted. Tests pin the heterogeneous-batch builder shape, the empty-row-excluded path, the mixed-action commit message, and the per-row UX end-to-end.
- ✓ Batch-size chip on `/admin/audit`. The `via ook-admin/<id>` trailer that every MCP write commit carries now optionally extends with a ` batch-size=N` field — emitted by `commitPatchBatch` only when the commit lands two or more patches (single-patch commits keep the bare trailer, so the common case looks unchanged). The audit parser reads the field into an optional `batchSize` on `AuditEntry.viaAdmin`, and the audit page renders a small "×N" chip next to the existing "via MCP" chip when `batchSize ≥ 2` — making a five-answer `/admin/backfill` "Send all" visit visually distinct from a single ad-hoc commit at a glance. Older commits without the field still parse cleanly (with `batchSize` undefined) so the audit row falls back to the plain MCP chip; idempotency is preserved (re-tagging a trailer that already carries `batch-size=N` is a no-op). Pinned by unit tests on emit (omitted for size 1, present for size ≥ 2, idempotent across both shapes), parse (round-trip, older-commit-no-size, malformed-token rejection), and the audit parser's surfacing of `batchSize` on the entry.
- ✓ `/discover` polish — score tooltip + regional-title dedupe. The score badge on each connection row now carries a `title=` attribute with the per-reason breakdown ("see-also (linked both ways) 6 + series (Discworld) 5 + author (Pratchett) 3 = 14") and a dotted-underline cue that the row is explorable on hover. `ConnectionReason.points` carries each reason's contribution from `scorePair` so the breakdown stays consistent with the score without re-deriving weights at render time. Tooltip is a plain HTML `title`, no JS island — works everywhere keyboards and touch screens land. The dedupe rule flags regional-title pairs (the UK Philosopher's Stone vs the US Sorcerer's Stone, etc.) and renders them as a single "Same book, different markets" row instead of letting them sit at the top of the list as a generic high-score connection. Rule: Dice coefficient on character bigrams ≥ 0.75 between normalised titles, see-also reciprocal in both directions, AND shared series membership with a matching `#N` index. The 0.75 threshold is looser than the spec's "> 85%" because the canonical Philosopher's/Sorcerer's case sits at ~0.79 by Dice — the structural conditions carry the discrimination, the title check is the sanity guard. Pure helpers in `src/lib/discover.ts` (`titleSimilarity`, `isRegionalTitlePair`, `dedupeRegionalTitles`, `formatScoreBreakdown`); `getConnections` wires the dedupe pass and the page wires the tooltip. Pinned by unit tests on the similarity measure (canonical pair clears 0.75, unrelated titles fail), the conjunctive rule (each missing leg rejects the pair), and the route-level mark-but-preserve-row contract.
- ✓ Ratings-over-time editorial caption on `/stats`. The rolling-average chart now carries a small italic kicker under the legend that names the bias in the data: I mostly log books I expect to love, so the post-2018 ~4.8 average reflects taste calibration rather than absolute quality. Editorial honesty — the chart describes how I've gotten better at picking, not how good the books are.
- ✓ Reading-days timeline fallback on `/stats/[year]`. The calendar heatmap renders as a near-empty grid for sparse years (the current year-to-date, or any year with very few logged events) and reads as a bug rather than a deliberate render. Below `HEATMAP_MIN_EVENTS` (20) the route now swaps in a horizontal timeline strip — a year-long rail with one dot per event, finishes filled, starts hollow, manual log entries as small ticks above the line. Dot radius scales with book length when the Hardcover cache has a `pages` count, falling back to a fixed size when it doesn't. At ≥ 20 events the calendar heatmap stays. New `getYearEvents(year)` lib helper sources event rows from frontmatter `started`/`finished` plus manual log entries, joined to the Hardcover-pages cache; new `YearEvent` type in `src/lib/types.ts`. Threshold-flip behaviour pinned by a vitest test that asserts which branch fires on either side of the boundary.
- ✓ `/shelf` overhaul. Spine width is now derived from each book's page count via `clamp(24, round(pages / 12), 72)` px (pure helper `computeSpineWidth` in `src/lib/shelf.ts`, unit-tested at the boundary cases), with a `pages?: number` frontmatter field added to the `Book` shape (`src/lib/types.ts`, `scripts/build-index.mjs` parse, the walk-fallback parser in `src/lib/books.ts`) and a 32 px fallback so the page is safe before `pages` lands across the corpus. Year separators — a 2 px gap and a small tick label on the bottom rail — interrupt the row at every finish-year change, only when sorted by finish date; the timeline metaphor is preserved (no row break). Books on the active bingo card carry a 2 px accent stripe along the top edge of the spine; currently-reading books carry a small bookmark tongue above the shelf line (currently-reading books now appear on the shelf alongside finished). Polish picks: author names dropped from under the spine entirely (now hover/tap-only in the tooltip), spine text flipped to top-to-bottom in the US/UK trade convention (head tilts right when the book lies flat), and the faint outline box replaced with a committed shelf edge — 1 px highlight on top, 2 px shadow on the bottom. Small legend under the shelf explains the size scale and the two markers.
- ✓ Bookmark-ribbon progress indicator on home-page currently-reading cards. New `parseProgress(progress, totalPages?)` pure helper at `src/lib/progress-parse.ts` pulls a 0–100 percent estimate out of the prose `progress` frontmatter field — recognises `47%` / `47 %`, `N of M` and `N/M` fractions, `p. 142` / `page 142` against the book's `Book.pages` total, and `chapter 5 of 20` chapter-fractions. Returns null when no pattern matches (most books don't have a parseable progress; the ribbon stays hidden then). New `ProgressRibbon` component (`src/components/ProgressRibbon.tsx`) renders the result as a thin accent-coloured silk ribbon hanging from the top edge of the Reading-now card with a small V-notch at the tip; ribbon length is `percent%` of the card height. The percent also surfaces as a `· N% through` tail on the existing days-in line so it's still readable for assistive tech. No new schema field — progress prose is what the reader's been writing all along; this just gives the renderer a way to interpret it. Pinned by unit tests across every pattern, the priority ordering between patterns when several appear in one string, the edge cases (over-100% clamp, degenerate denominator, missing totalPages context, date-like substrings), and the non-match path.
- ✓ URL-paste enrichment in the `/admin` agent. Pasting a Goodreads / Hardcover / Storygraph / Amazon / Bookwyrm book URL into the free-text input pre-parses the platform IDs out of the URL and appends them as a parenthetical at the end of the message before the LLM sees it — so the agent treats them as ground-truth fields ready to write to frontmatter without a separate tool call. Pure URL → IDs (regex, no network) in `src/lib/url-id-detect.ts` (`parseBookIds`); enrichment wrapper `enrichWithUrlIds` in `src/lib/admin/agent.ts` runs once per `runAgent` call before the user text becomes a message. Supports `goodreads_id`, `hardcover_slug`, `storygraph_slug`, `bookwyrm_url` (all four are schema fields the agent can write today) plus `amazon_asin`, `isbn10`, `isbn13`, and `titleHint` (surfaced for the agent to mention, not yet schema-backed). Amazon's `/dp/<ASIN>` and `/gp/product/<ASIN>` shapes both supported; 10-digit ASINs that pass the ISBN-10 checksum surface as `isbn10` too (one paste, two IDs). Tests pin every platform shape, the negative cases (non-book URLs, malformed input), and the agent-side enrichment behaviour (parenthetical shape, multi-URL handling, trailing-punctuation strip, unrecognised-URL passthrough).
- ✓ Pullquote auto-suggester on `/admin/backfill`. Finished books with a `quotes.md` and no `pullquote` set become a new card kind that offers the top three lines from quotes.md as one-tap picker buttons. Pure scoring helper at `src/lib/admin/pullquote-suggester.ts` parses the file's blockquote paragraphs (merging consecutive `> ` lines, attaching following `—` lines as `source`), then scores each candidate on a length-curve peaked at ~120 chars, sentence-completeness (starts capital, ends with terminal punctuation), single-sentence preference, and an attribution-presence bonus. The card renders the top three (or fewer when the file is thin), and one click stages a `pullquote: { text, source }` frontmatter patch through the existing Stage / Send-all batch flow. No external sources — operates only on the reader's own quotes.md. New kind ordered FIRST in the round-robin (highest voice surface — the reader committed lines to quotes.md, picking the headline is pure curation). Tests pin the parser shape, the scoring rubric, top-N selection, and the card's canAnswer / buildPatch wire-format contract.
- ✓ Wikiquote backfill + `/admin/community-quotes` review surface. New `scripts/backfill-wikiquotes.mjs` queries Wikiquote's public MediaWiki API (`action=parse&prop=wikitext`) for each finished vault book and caches up to ten community quotes per book in `_meta/wikiquotes.json`. Title-variant fallback walks `<Title>` → `<Title> (novel)` → `<Title> (book)` → `<Title> (<Author>)` → `<Title> (<Surname>)` until a page returns a non-empty Quotes section. Wikitext extraction strips `{{templates}}`, `<ref>...</ref>`, wiki-links, and bold/italic markup; `**`-prefixed lines following a quote attach as `source`. Rate-limited at ≥ 1100 ms per request, polite default for a free public API. Operator-run via `make vault-wikiquotes`. The cached content is **never rendered publicly** — by deliberate design, honouring the user constraint that any community/internet-sourced quote gets owner approval before it lands. The new passkey-gated `/admin/community-quotes` page is read-only: lists each cached book with its quotes and a "via Wikiquote → <variant>" deep link; no copy / publish / auto-attach actions on this surface. The operator reviews and copies anything worth keeping into their own pullquote (frontmatter) or quotes.md by hand. AdminConsole gains a "Community quotes →" link alongside the existing Backfill / Audit links. Tests pin every extractor branch (templates, refs, wiki-links, bold/italic, HTML, whitespace collapsing), the Quotes-section finder (canonical / Quotations / Selected-quotes heading variants), the section-stop behaviour, the title-variant fallback ordering, and the `**`-line attribution merge.
- ✓ Kindle reading-session import end-to-end. `scripts/import-kindle-sessions.mjs` reads an Amazon privacy-data takeout (`Kindle.Devices.ReadingSession.csv` + `Digital.Content.Ownership/*.json`), joins on ASIN, and writes a per-ASIN **summary** to `_meta/kindle-sessions.json` carrying `title`, `acquiredDate`, `resourceType`, `sessions`, `totalSeconds`, `firstStart`, `lastEnd`, `distinctDays`. Raw session rows never enter git — the takeout's 12k+ rows collapse to ~76 KB of summary at script time. New `amazon_asin: string | null` frontmatter field threaded through `src/lib/types.ts`, `scripts/build-index.mjs`, the walk-fallback parser in `src/lib/books.ts`, and the MCP-tool projection in `src/lib/mcp/book-tools.ts`; Amazon is intentionally absent from the external-links row (the ASIN is for session lookup, not a "Buy on Amazon" affordance). `scripts/backfill-asin-from-sessions.mjs` reads the summary, normalises both Kindle and vault titles (strips trailing parentheticals, `:` / `-` subtitles, folds smart quotes and dashes), exact-matches on either form, and prompts to stamp `amazon_asin:` onto matched vault books. Session-count tiebreak on multi-edition collisions. New `loadKindleSessions()` in `src/lib/books.ts` joins the summary into each `BookPage` via `book.amazonAsin` and the per-book page (`/books/[slug]`) renders a discreet "Read across N sessions over D days · ~Xh total · on Kindle" line under the existing Hardcover-stats slot, hidden entirely when the book has no ASIN or no cache hit. Pure helpers in `scripts/lib/kindle-sessions.mjs` and `scripts/lib/asin-match.mjs`; ~50 unit tests pin CSV parsing edge cases (BOM, no-start-timestamp drops, malformed rows), ownership-shard filtering (`KindleEBook` / `KindlePDoc` kept, samples and `MobileApp` dropped), earliest-`acquiredDate` selection, title normalisation, the multi-edition session-count tiebreak, and the surgical `insertField` for the `amazon_asin` line.
- ✓ Pages backfill from the Hardcover cache. `scripts/backfill-pages.mjs` ferries the cached per-book page count from `_meta/hardcover-books.json` into each vault book's `pages:` frontmatter as a bare integer. Pure cache-to-frontmatter; no network at runtime. Per-book skip: an existing positive-integer `pages` value is user-authoritative and never overwritten, so manual corrections stick. Cache values of `null` or `0` are treated as missing — those books stay unwritten until Hardcover has real page data. Operator-run via `make vault-pages` (the cache lookup remains operator-initiated via `make vault-hardcover-books`). Unblocks the `/shelf` page-count-driven spine width: every book with a Hardcover record now gets a width-from-pages instead of falling back to the 32 px default.
- ✓ Honest reading-dates row on per-book pages. New `BookDates` sub-row under the header surfaces `started` and `finished` as a small dot-separated line for `finished` / `paused` / `abandoned` books. Four shapes covered explicitly: both dates present (`started 2024-06-01 · finished 2024-06-15`), started only (`started 2024-06-01 · finished date unknown`), finished only (`start date unknown · finished 2024-06-15`), and neither (`dates unknown`). Verb tracks the status (`finished` / `paused` / `abandoned`). Suppressed entirely for `reading` and `tbr` — those statuses have no reading-history register to make honest. Closes the gap where a finished book with `finished: null` rendered no date at all and the reader couldn't tell whether we didn't know it or just weren't showing it. The Stamp's "READ" fallback (postal-flourish SVG next to the cover) stays — it's a separate visual register.
- ✓ Series roster filter — canonical English titles only. `_meta/series-rosters.json` is sourced from Hardcover's GraphQL which returns every edition for each series — translations, edition variants, and companion volumes alongside the canonical novels. The `/series` renderer now runs each roster through `filterCanonicalRosterBooks` (in `src/lib/books.ts`) before counting members and rendering missing-from-vault entries: rule 1 drops non-integer position entries (1.1 / 2.3 are translation variants of the integer-position canonical), rule 2 drops integer-position entries whose title is clearly non-English — non-Latin script (Cyrillic, Greek, etc.), Slavic-only diacritics (`ąęłńśźżČčŠšŽžŘř`), or a capitalised non-English leading article (`El `, `La `, `Le `, `Der `, `Die `, `L'`, etc.). Curly apostrophes, em/en dashes, and middle dots are allow-listed so titles like "The Handmaid's Tale" stay. ASoIaF: 23 → 6. Stormlight: 25 → 10. Dune: 17 → 6. Mistborn: 13 → 10. Discworld: 49 → 41 (drops the half-position chapbook short-stories). Trade-off worth surfacing: the integer-only rule also drops legit English half-position entries like "Mistborn: Secret History" (3.5); follow-up rule could narrow rule 1 to "drop fractional only when its integer-floor exists as a separate entry" if those losses bite. Pinned by unit tests covering both filter rules and the typography allow-list.
- ✓ Tag co-occurrence drill-in — `/tags/[tag]?and=<other>` boolean-AND view. The "Strongest pairings" rows on `/tags` are now clickable into the matching intersection (each row carries an underlined count + bar group that navigates to `/tags/[a]?and=[b]`); the unfiltered `/tags/[tag]` page gains a quiet "Also tagged with" row of drill-in chips for its top-3 co-occurring tags. The AND view swaps the kicker to "Tag intersection", renders both tag names in the H1 (each a back-link to its own single-tag page), and uses an alternate body copy ("N books carrying both X and Y"). Empty intersections render an inline empty-state rather than 404 — the user navigated here deliberately, "no books carry both" is a valid answer. Self-AND (`?and=<same-tag>`) degenerates to the single-tag view. `BackLink` swaps to "back to <tag>" when filtered. Pure helper `intersectBooksByTags` (`src/lib/tag-intersection.ts`) over a `Pick<Book, "tags">` shape; OR is explicitly out of scope until a felt need surfaces.
- ✓ Click-into-info pass — linked stats and parent-index back-links. The home page's `StatsStrip` tiles now drill in (`Reading` → `/now`, `Finished` → `/log`, `Bingo X/Y` → in-page `#bingo` anchor); the `/stats/[year]` Topline anchors `Finished` to the same-page `#the-wall` cover mosaic and `Avg rating` to `#rating-distribution`. A new `BackLink` component (top-of-page parent-index arrow, decorative chrome matching `HomeMark` at a quieter register) lands on `/tags/[tag]` (→ `/tags`), `/stats/[year]` (→ `/stats`), and `/print/[year]` (→ `/stats/[year]`, wrapped in `print:hidden`). Stats that have no clean drill-in route (`/stats/[year]` Started / Abandoned / Would-reread / Most-read-authors; `/books/[slug]` and `/series/[slug]` parent-index links) stay unlinked rather than ship broken targets — designing filtered list surfaces for those is a deferred call. Implements the "informative counts should be clickable, link style should be subtle" rule (inline links inherit text style, no `text-accent` paint; decorative top-of-page arrows are the exception).
- ✓ Per-book Kindle session line — human-readable date range. The "Read across N sessions over D days · ~Xh total · on Kindle" line under per-book metadata now carries a trailing date range derived from `KindleStats.firstStart` / `.lastEnd`: same-month renders as `Apr 2 → Apr 24`, cross-year as `Dec 30, 2024 → Jan 5, 2025`, single-day reads collapse to `Mar 15`. Replaces the previous raw-ISO render that sat in `font-mono` adjacent to the line. Same `text-ink-dim` as the "on Kindle" label; no font change, no styling chrome. Local `formatKindleRange` helper on the per-book page; matches the idiom used by `prettyWeek` in the changelog.
- ✓ Historical-reach heatmap backdrop on `/stats` + `/stats/[year]`. `scripts/import-kindle-sessions.mjs` now writes two extra projections alongside the per-ASIN summary: a top-level `dailyCounts` map (YYYY-MM-DD → session count, local-time attributed via `scripts/lib/dates.mjs::isoToLocalDate`) and a pre-summed `unlinkedSessions: { sessions, totalSeconds }` for the unlinked-activity footnote. Both projections land in the same `_meta/kindle-sessions.json` file — single-file schema keeps one read per request and one diff per re-import. New `loadKindleDailyCounts()` in `src/lib/books.ts` feeds `getYearActivity(year)` and `getStatsYears()`; `DayActivity` gains a `kindleSessions: number` field distinct from `count` so the year heatmap can render Kindle-only days as a faint backdrop layer (color-mix on `--good`, ~14%) without competing with the event ramp. Cells with both an event and a Kindle session render the event colour — events win the foreground. Threshold flip to the timeline fallback now treats event-count + Kindle-day-count as combined signal so Kindle-only years (pre-vault-era) hit the calendar render path. Timeline strip renders Kindle-only days as small faint dots beneath the rail. `/stats` index lights up Kindle-only years as faint "N Kindle reading days" tiles. `getUnlinkedKindleActivity()` prefers the new top-level projection with a per-record fallback for older cache shapes — render-side changes degrade gracefully when either projection is absent.
- ✓ Start-flow trigger prompt on `/admin`. When the user reports starting a book — a tbr → reading status flip — the admin agent asks one plain-text follow-up: "What brought you to this?" The answer lands in a new `trigger:` frontmatter field bundled into the same `propose_patch` as the status flip, so one commit captures motive + state together. Skippable in one tap (reply "skip" / "no" / silence → status flip lands without a trigger); operator-overrideable in the initial free-text ("starting Piranesi, picked it up because Susanna Clarke was recommended" → no follow-up). Once per book — second-reads skip the prompt entirely, detected via the book already carrying a `started` or `finished` value. New `trigger: string | null` field on the `Book` type threaded through `src/lib/types.ts`, the `scripts/build-index.mjs` parse, the walk-fallback parser in `src/lib/books.ts`, the MCP-tool projection in `src/lib/mcp/book-tools.ts`, and the `/schema` summary in `src/lib/schema.ts`. Renders on `/books/[slug]` as a small italic "why I picked this: …" line in the metadata strip, hidden entirely when absent. Pure helper `shouldAskStartTrigger(book)` is exported for test pinning; the natural-language rule itself is pinned by `admin-agent-prompt.test.ts` against a `START_FLOW_INSTRUCTION_MARKER` constant, mirroring the finish-flow gate's discipline.
- ✓ 5-star unreviewed opportunistic prompt on the `/admin` console. When the agent has staged a patch and the user is about to commit, the patch-staged surface fetches one candidate from the pool of `status: finished + rating: 5 + no review.md` books and renders a small dashed-border prompt under the diff preview: "Quick — why was _<Title>_ a five?". A skippable textarea seeds `review.md`; typing a sentence or two flips the commit button to "Commit (with review)" and bundles a second `CommitPatchInput` against the offered slug into a single `/api/admin/agent/commit-batch` write so the original patch and the new `<slug>/review.md` land as one atomic commit. Skipping or committing-without-an-answer records the slug in component state so it isn't re-offered this session (state evaporates on tab close, which IS session end). The prompt is suppressed when the staged patch is FOR the 5-star candidate itself (the agent might be writing the review) and stays hidden when the pool is empty. New pure helper at `src/lib/admin/five-star-unreviewed.ts` (corpus filter + session-aware `pickOne`); auth-gated `GET /api/admin/five-star-unreviewed?exclude=<slug,...>` returns one candidate or `null`. Tests pin the filter rule (finished + rating=5 + !hasReview), the public-catalog-only projection (no `progress` leakage), the session-tracking walk through the pool, the prompt fetch on patch-staged entry, the skip → next-fetch exclusion threading, and the wire-format of the bundled commit-batch body.
- ✓ Public `GET /api/showcase.json` reading-showcase feed. An unauthenticated JSON endpoint that an external personal site (vhata.net) consumes server-side to feature the owner's reading without scraping Goodreads. Returns `nowReading` (currently-reading, most-recent-first by activity anchor — same auto-paused-aware set as `/now`'s Reading section, via `getCurrentlyReading`), `recentlyFinished` (last five finished, via `getRecentlyFinished`), `bingo` (the current card's filled/total mirroring the home page's free-square-excluded count, or null), `stats.booksThisYear` (via `getYearStats`), and the absolute `siteUrl`. All book URLs are absolute and slug-encoded; covers pass through as the absolute external URLs already in frontmatter; `progressPercent` reuses the home-page `parseProgress` helper (null when the prose isn't parseable); ratings round + clamp to integer 1-5. The payload field names are a stability contract with the consuming site — additive changes are safe, renames need coordination. Sparse data degrades to empty arrays / null fields rather than erroring. `Cache-Control: s-maxage=300` (plus a harmless `Access-Control-Allow-Origin: *`) matches the consumer's ~5-minute ISR window. Shaping logic is the pure `buildShowcase` in `src/lib/showcase.ts` (reused accessors wired in `getShowcase`); the route at `src/app/api/showcase.json/route.ts` is thin glue. Pinned by unit tests on the shaping rules (URL encoding, author join, progress derivation, recency sort, rating round/clamp, free-square-excluded bingo count, sparse-corpus shape) and a route test on the HTTP contract (200, JSON, cache header).
